// JavaScript Example for Navigation Patterns
// Function to toggle the navigation drawer (Discussed in the "Tab bars and navigation drawers in mobile apps" section)
function toggleDrawer() {
    var drawer = document.getElementById("navigation-drawer");
    drawer.style.width = drawer.style.width === "250px" ? "0" : "250px";
}
